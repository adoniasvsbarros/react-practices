import React from 'react';

export default function First() {
    const msg = "Welcome back!";

    return (
        <div>
            <h2>First Component</h2>
            <p>{ msg }</p>
        </div>
    );
}

